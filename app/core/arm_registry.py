"""Backward-compatibility shim — real implementation in app.core.hardware."""

from app.core.hardware.types import MotorType, ArmRole, ConnectionStatus, ArmDefinition, Pairing
from app.core.hardware.arm_registry import ArmRegistryService
